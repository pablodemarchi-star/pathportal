# Brand Logo Asset

The exact authorised Illustrator source is stored here without editing, redrawing, recolouring or compressing:

```text
path-logo-original.ai
```

The web image used by the app is:

```text
path-logo-web.png
```

The white transparent image used on dark backgrounds is:

```text
path-logo-white-transparent.png
```

These files are generated from the original `.ai` preview so the platform can display the logo in a browser. Do not redraw, trace, recolour or stretch the logo.
